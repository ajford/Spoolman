version: '3.8'
services:
  spoolman:
    image: ${SPOOLMAN_REPOSITORY_BASE}:test
    environment:
      - SPOOLMAN_LOGGING_LEVEL=INFO
      - TZ=Europe/Stockholm
  tester:
    image: ${SPOOLMAN_REPOSITORY_BASE}-tester:latest
    volumes:
      - ./tests:/tester/tests
    environment:
      - DB_TYPE=sqlite
    depends_on:
      - spoolman
